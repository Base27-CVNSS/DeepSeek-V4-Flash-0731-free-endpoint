{
  "name": "@base27/free-llm-router",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "OpenAI-compatible free-tier LLM gateway with provider failover and a trilingual chat UI.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/router.js && node --check src/providers.js && node --check public/app.js && npm test"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
